services:
  react-app:
    image: node:20
    container_name: react-app
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8080:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

