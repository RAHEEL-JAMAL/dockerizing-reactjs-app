React App (Dockerized)

This is my React app that I containerized using Docker. I also added screenshots just to show how the project looks when running.

How to Run (Docker Compose) >>>>>

Just run:

docker-compose up --build -d
